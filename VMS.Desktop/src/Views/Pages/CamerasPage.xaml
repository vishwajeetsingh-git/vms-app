<UserControl x:Class="VMS.Views.Pages.CamerasPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:VMS.Converters">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter        x:Key="BoolToVisibility"/>
        <conv:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility"/>

        <!-- Status dot style -->
        <Style x:Key="StatusDot" TargetType="Ellipse">
            <Setter Property="Width"  Value="8"/>
            <Setter Property="Height" Value="8"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Action button style — small, flat -->
        <Style x:Key="ActionBtn" TargetType="Button">
            <Setter Property="Height"          Value="26"/>
            <Setter Property="Padding"         Value="10,0"/>
            <Setter Property="FontSize"        Value="11"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.70"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListView item hover/selected -->
        <Style TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding"                    Value="0"/>
            <Setter Property="Background"                 Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0,0,0,1"
                                BorderBrush="{DynamicResource BorderColor}">
                            <GridViewRowPresenter Margin="0,8"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource MenuHoverColor}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource MenuActiveColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{DynamicResource BackgroundColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ═══ Header ═══ -->
        <Border Grid.Row="0"
                Background="{DynamicResource SurfaceColor}"
                BorderThickness="0,0,0,1"
                BorderBrush="{DynamicResource BorderColor}"
                Padding="24,16">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Cameras" FontSize="18" FontWeight="SemiBold"/>
                    <Border Background="{DynamicResource CardBackgroundColor}"
                            CornerRadius="10" Margin="10,0,0,0"
                            Padding="8,2" VerticalAlignment="Center">
                        <TextBlock FontSize="11" Foreground="{DynamicResource TextSecondaryColor}">
                            <Run Text="{Binding TotalCount, Mode=OneWay}"/>
                            <Run Text="total ·"/>
                            <Run Text="{Binding OnlineCount, Mode=OneWay}"
                                 Foreground="{DynamicResource SuccessColor}"/>
                            <Run Text="online"/>
                        </TextBlock>
                    </Border>
                </StackPanel>

                <Button Content="+ Add Camera" HorizontalAlignment="Right"
                        Width="120" Height="32"
                        Command="{Binding AddCommand}"/>
            </Grid>
        </Border>

        <!-- ═══ Toolbar ═══ -->
        <Border Grid.Row="1" Padding="24,10"
                Background="{DynamicResource SurfaceColor}"
                BorderThickness="0,0,0,1"
                BorderBrush="{DynamicResource BorderColor}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MaxWidth="360"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search -->
                <Border Grid.Column="0"
                        Background="{DynamicResource InputBackgroundColor}"
                        BorderBrush="{DynamicResource BorderColor}"
                        BorderThickness="1" CornerRadius="4">
                    <Grid>
                        <!-- Placeholder: Collapsed by default, Visible only when SearchText is "" -->
                        <TextBlock Text="🔍  Search cameras…"
                                   IsHitTestVisible="False"
                                   Foreground="{DynamicResource TextSecondaryColor}"
                                   FontSize="12" Margin="10,0,0,0"
                                   VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SearchText}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBox Background="Transparent" BorderThickness="0"
                                 Height="30" Padding="10,0"
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>
                </Border>

                <!-- Loading indicator -->
                <TextBlock Grid.Column="1" Text="Loading…"
                           Foreground="{DynamicResource TextSecondaryColor}"
                           FontSize="12" VerticalAlignment="Center" Margin="16,0,0,0"
                           Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"/>

                <!-- Refresh -->
                <Button Grid.Column="2" Content="↺  Refresh"
                        Height="30" Width="90"
                        Command="{Binding RefreshCommand}"/>
            </Grid>
        </Border>

        <!-- ═══ Camera list ═══ -->
        <Grid Grid.Row="2">

            <!-- Empty state — only when not loading AND list is empty -->
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsLoading}"      Value="False"/>
                                    <Condition Binding="{Binding Filtered.Count}" Value="0"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible"/>
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <TextBlock Text="📷" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                <TextBlock Text="No cameras found" FontSize="15" FontWeight="SemiBold"
                           HorizontalAlignment="Center" Margin="0,0,0,6"/>
                <TextBlock Text="Add a camera using the button above."
                           Foreground="{DynamicResource TextSecondaryColor}"
                           FontSize="12" HorizontalAlignment="Center"/>
            </StackPanel>

            <ListView ItemsSource="{Binding Filtered}"
                      SelectedItem="{Binding Selected}"
                      Background="Transparent"
                      BorderThickness="0"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto">
                <ListView.View>
                    <GridView>

                        <!-- Status -->
                        <GridViewColumn Header="Status" Width="80">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Margin="0,0,6,0">
                                            <Ellipse.Style>
                                                <Style TargetType="Ellipse" BasedOn="{StaticResource StatusDot}">
                                                    <Setter Property="Fill" Value="{DynamicResource TextSecondaryColor}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsOnline}" Value="True">
                                                            <Setter Property="Fill" Value="{DynamicResource SuccessColor}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding StatusText}" Value="Offline">
                                                            <Setter Property="Fill" Value="{DynamicResource ErrorColor}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Ellipse.Style>
                                        </Ellipse>
                                        <TextBlock Text="{Binding StatusText}"
                                                   FontSize="12"
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Name -->
                        <GridViewColumn Header="Name" Width="180">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}"
                                               FontWeight="SemiBold" FontSize="13"
                                               TextTrimming="CharacterEllipsis"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Protocol badge -->
                        <GridViewColumn Header="Protocol" Width="80">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="3" Padding="6,2"
                                            Background="{DynamicResource CardBackgroundColor}">
                                        <TextBlock Text="{Binding ProtocolBadge}"
                                                   FontSize="10" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource SecondaryColor}"/>
                                    </Border>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Connection info -->
                        <GridViewColumn Header="Connection" Width="220">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock FontSize="12"
                                               Foreground="{DynamicResource TextSecondaryColor}"
                                               TextTrimming="CharacterEllipsis">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="{Binding RtspUrl}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Protocol}" Value="onvif">
                                                        <Setter Property="Text" Value="{Binding OnvifHost}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- VR Server -->
                        <GridViewColumn Header="VR Server" Width="90">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding VRServerId}" FontSize="12"
                                               Foreground="{DynamicResource TextSecondaryColor}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Location -->
                        <GridViewColumn Header="Location" Width="120">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Location}" FontSize="12"
                                               Foreground="{DynamicResource TextSecondaryColor}"
                                               TextTrimming="CharacterEllipsis"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Actions -->
                        <GridViewColumn Header="Actions" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">

                                        <!-- Record / Stop -->
                                        <Button Style="{StaticResource ActionBtn}"
                                                Content="{Binding RecordingText}"
                                                Margin="0,0,6,0"
                                                Background="{DynamicResource WarningColor}"
                                                Command="{Binding DataContext.ToggleRecordingCommand,
                                                    RelativeSource={RelativeSource AncestorType=ListView}}"
                                                CommandParameter="{Binding}"/>

                                        <!-- Edit -->
                                        <Button Style="{StaticResource ActionBtn}"
                                                Content="Edit" Margin="0,0,6,0"
                                                Background="{DynamicResource PrimaryColor}"
                                                Command="{Binding DataContext.EditCommand,
                                                    RelativeSource={RelativeSource AncestorType=ListView}}"
                                                CommandParameter="{Binding}"/>

                                        <!-- Delete -->
                                        <Button Style="{StaticResource ActionBtn}"
                                                Content="Delete"
                                                Background="{DynamicResource ErrorColor}"
                                                Command="{Binding DataContext.DeleteCommand,
                                                    RelativeSource={RelativeSource AncestorType=ListView}}"
                                                CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                    </GridView>
                </ListView.View>

                <!-- GridView column header style — full ControlTemplate to own hover/pressed states -->
                <ListView.Resources>
                    <Style TargetType="GridViewColumnHeader">
                        <Setter Property="Background"              Value="{DynamicResource SurfaceColor}"/>
                        <Setter Property="Foreground"              Value="{DynamicResource TextSecondaryColor}"/>
                        <Setter Property="FontSize"                Value="10"/>
                        <Setter Property="FontWeight"              Value="SemiBold"/>
                        <Setter Property="Padding"                 Value="12,6"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="GridViewColumnHeader">
                                    <Grid>
                                        <Border x:Name="Bd"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{DynamicResource BorderColor}"
                                                BorderThickness="0,0,0,1"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter
                                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                VerticalAlignment="Center"
                                                RecognizesAccessKey="True"/>
                                        </Border>
                                        <!-- Column resize gripper -->
                                        <Thumb x:Name="PART_HeaderGripper"
                                               HorizontalAlignment="Right"
                                               Width="8" Cursor="SizeWE"
                                               Opacity="0"/>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background"
                                                    Value="{DynamicResource CardBackgroundColor}"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="Bd" Property="Background"
                                                    Value="{DynamicResource MenuHoverColor}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.Resources>
            </ListView>
        </Grid>

    </Grid>
</UserControl>
